---
interface Props { command: string; label?: string }
const { command, label } = Astro.props;
---
<figure class="doc-command">
  {label && <figcaption>{label}</figcaption>}
  <pre><code>{command}</code></pre>
</figure>
